# Backend server URL (no trailing slash)
# In development, the Next.js dev proxy handles /api/* → localhost:5000
# so this env var is only needed for direct SSR fetches or non-proxied calls.
NEXT_PUBLIC_API_URL=http://localhost:5000
